import { createManualCard } from "@/app/catalogo/actions";

export const dynamic = "force-dynamic";

export default async function CatalogPage({ searchParams }: { searchParams: Promise<{ error?: string; message?: string }> }) {
  const params = await searchParams;
  return <section className="space-y-6"><div><h1 className="text-2xl font-black">Catálogo</h1><p className="mt-2 text-sm text-slate-500">Cadastre cartas manualmente enquanto os providers externos são validados.</p></div>{params.error && <p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">Não foi possível cadastrar a carta.</p>}{params.message && <p className="rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">Carta cadastrada com sucesso.</p>}<form action={createManualCard} className="grid gap-4 rounded-2xl border border-slate-200 bg-white p-5 sm:grid-cols-2"><label className="text-sm font-semibold">Nome<input className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" name="name" required /></label><label className="text-sm font-semibold">Número<input className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" name="card_number" /></label><label className="text-sm font-semibold">Coleção/edição<input className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" name="set_name" /></label><label className="text-sm font-semibold">Idioma<select className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" defaultValue="pt-BR" name="language"><option>pt-BR</option><option>en</option><option>ja</option></select></label><label className="text-sm font-semibold">Variante<input className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" defaultValue="normal" name="variant" /></label><label className="text-sm font-semibold">Raridade<input className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" name="rarity" /></label><button className="rounded-xl bg-ink px-4 py-3 font-bold text-white sm:col-span-2" type="submit">Cadastrar carta</button></form></section>;
}
