import { signIn } from "@/app/auth/actions";

export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string; message?: string }> }) {
  const params = await searchParams;
  return <main className="mx-auto flex min-h-[70vh] max-w-md items-center"><section className="w-full rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm font-bold text-accent">PokeBinder</p><h1 className="mt-2 text-2xl font-black">Entrar na sua coleção</h1>{params.error && <p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">Não foi possível entrar. Confira os dados e tente novamente.</p>}{params.message && <p className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">Confira seu e-mail para continuar.</p>}<form action={signIn} className="mt-6 space-y-4"><label className="block text-sm font-semibold">E-mail<input className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" name="email" required type="email" /></label><label className="block text-sm font-semibold">Senha<input className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" name="password" required type="password" /></label><button className="w-full rounded-xl bg-ink px-4 py-3 font-bold text-white" type="submit">Entrar</button></form><p className="mt-5 text-center text-sm text-slate-500">Ainda não tem conta? <a className="font-bold text-ink" href="/cadastro">Criar cadastro</a></p></section></main>;
}
