import { signUp } from "@/app/auth/actions";

export default async function RegisterPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const params = await searchParams;
  return <main className="mx-auto flex min-h-[70vh] max-w-md items-center"><section className="w-full rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm font-bold text-accent">PokeBinder</p><h1 className="mt-2 text-2xl font-black">Criar sua conta</h1>{params.error && <p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">Confira os dados. A senha deve ter pelo menos 8 caracteres.</p>}<form action={signUp} className="mt-6 space-y-4"><label className="block text-sm font-semibold">Nome de usuário<input className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" name="username" pattern="[a-zA-Z0-9_]{3,30}" required /></label><label className="block text-sm font-semibold">Nome<input className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" name="display_name" required /></label><label className="block text-sm font-semibold">E-mail<input className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" name="email" required type="email" /></label><label className="block text-sm font-semibold">Senha<input className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-3" minLength={8} name="password" required type="password" /></label><button className="w-full rounded-xl bg-ink px-4 py-3 font-bold text-white" type="submit">Criar conta</button></form><p className="mt-5 text-center text-sm text-slate-500">Já tem conta? <a className="font-bold text-ink" href="/login">Entrar</a></p></section></main>;
}
